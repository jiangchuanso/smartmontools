# 2026-08-27

## 修复 el8 RPM 构建工作流 bug

检查并修复了最近提交（f80642b9 "增加编译el8的rpm工作流"）引入的问题：

1. **浅克隆 bug**（`.github/workflows/build-centos8-rpm.yml`）：
   - 仓库共 6217 提交，原 `fetch-depth: 3500` 浅克隆会漏掉 smartmontools 7.5 基线提交 `943adaeda55c2d534c722fe66c6b4613a782caa1`。
   - configure.ac 中 `smartmontools_release_date` 为空 → **pre-release 构建**，`util/getversion.sh` 需该基线计算 svn 修订号与提交计数；缺失时版本号退化为 `8.0-gREV`（丢失提交数）。
   - 修复：`fetch-depth: 0`（完整克隆）。

2. **硬编码版本号**：`cp -v smartmontools-8.0.tar.gz` → `cp -v smartmontools-*.tar.gz`（版本升级后原命令会失败）。

3. **缺 `systemd-rpm-macros`**：spec 用 `%systemd_post`/`%{_unitdir}` 宏，已补 `BuildRequires` 并在 workflow 的 dnf install 中显式安装。

已核对的正确项：configure.ac 版本 8.0 与 spec 一致；`%files` 所有路径与实际安装位置匹配（drivedb.h→/usr/share/smartmontools、smartd_warning.sh→/etc、smartd.service→/usr/lib/systemd/system、doc 文件全部存在）；`m4/` 由 autogen.sh 的 aclocal --install 生成，非缺失。
